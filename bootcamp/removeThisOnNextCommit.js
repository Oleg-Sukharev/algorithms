function steps(n) {
  let counter = 1;
  const results = Array.from({ length: n }).fill([]);
  const startCol = 0;
  const endCol = n - 1;
  const row = 0;
  const endRow = n - 1;



  console.log(results);
}
console.log(steps(3)); 